sudo apt-add-repository ppa:qameta/allure
sudo apt-get update 
sudo apt-get install allure
        OR
pip install allure-pytest

also install allure-command line tool

After script run completed run "allure serve 'path to report folder'" to view allure report


